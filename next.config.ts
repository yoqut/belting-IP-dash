import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: [
    "thoroughly-mandate-tribal-bizrate.trycloudflare.com",
    "belting-ip-dash.vercel.app"
  ],
};

export default nextConfig;
